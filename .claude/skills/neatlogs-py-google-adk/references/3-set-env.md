# Step 3: Set Environment Variables

1. `set_env_values`: `NEATLOGS_API_KEY`.
2. Preserve `.env.example` keys: `GOOGLE_API_KEY` (and `GOOGLE_GENAI_USE_VERTEXAI=0` for AI-Studio keys). `.env` is a superset.
3. `.env` gitignored.
