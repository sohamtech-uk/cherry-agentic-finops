(() => {
  "use strict";

  const REQUEST_COOLDOWN_MS = 5000;
  const state = { files: [], sources: null, analysis: null, busy: false, lastRequestAt: new Map() };
  const TYPE_LABELS = {
    nav_workbook: "NAV workbook", investor_gl: "Investor-level GL", lp_commitments: "LP commitments",
    bank_statement_working_file: "Bank statement working file", loader_template: "Loader / mapping workbook",
    capital_call_notice: "Capital-call notice", lpa: "Limited Partnership Agreement", side_letter: "Side letter",
    bank_statement: "Bank statement", financial_statement: "Financial statements", investor_report: "Investor report",
    positions: "Positions", trades: "Trades", bank_transactions: "Bank transactions", cash_transactions: "Cash transactions"
  };
  const q = (selector, root = document) => root.querySelector(selector);
  const qa = (selector, root = document) => [...root.querySelectorAll(selector)];
  const esc = (value) => String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;");
  function notify(message, error = false) { if (typeof toast === "function") { toast(message, error); return; } if (error) console.error(message); }
  function busy(visible) { state.busy = visible; if (typeof loading === "function") loading(visible); }
  function injectStyles() { if (q('link[href="/static/fund_manager.css"]')) return; const link = document.createElement("link"); link.rel = "stylesheet"; link.href = "/static/fund_manager.css"; document.head.appendChild(link); }
  function humanSize(bytes) { if (bytes < 1024) return `${bytes} B`; if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`; return `${(bytes / (1024 * 1024)).toFixed(1)} MB`; }
  function typeLabel(t) { if (TYPE_LABELS[t]) return TYPE_LABELS[t]; if (t === "unknown_workbook") return "Unknown workbook"; if (t === "unknown_pdf") return "Unknown document"; if (t === "unknown_json" || t === "unknown_csv") return "Unknown data file"; return "Unknown file"; }
  function statusPill(status) { if (status === "processed") return '<span class="fm-pill ok">Identified</span>'; if (status === "unreadable") return '<span class="fm-pill error">Unreadable</span>'; return '<span class="fm-pill review">Needs review</span>'; }
  function shellMarkup() { return `<section class="fm-shell" id="fund-manager" aria-labelledby="fm-title"><div class="fm-inner"><div class="fm-head"><p class="fm-kicker">Cherry Fund Manager · NAV reconciliation</p><h2 id="fm-title">Upload fund evidence.<br><em>Cherry works out what it is.</em></h2><p>Add any mix of NAV workbooks, investor GLs, capital-call notices, LPAs, side letters, bank statements or position/cash/trade files. The agent identifies each source, selects the required deterministic controls and investigates any resulting exceptions.</p></div><div class="fm-card"><div class="fm-dropzone" id="fm-dropzone"><span>PDF · XLSX · CSV · JSON — drop files here or</span><button type="button" class="fm-button secondary" id="fm-browse">Browse files</button><small>Mixed file types in one review are fine. Nothing is required in advance.</small><input type="file" id="fm-file-input" multiple accept=".pdf,.xlsx,.xls,.csv,.json"></div><div class="fm-file-list" id="fm-file-list" aria-live="polite"></div><div class="fm-actions"><button type="button" class="fm-button primary" id="fm-analyse" disabled>Analyse</button><button type="button" class="fm-button ghost" id="fm-clear-files" hidden>Clear all</button><span class="fm-file-count" id="fm-file-count"></span></div><div id="fm-inventory" aria-live="polite"></div><div id="fm-analysis" aria-live="polite"></div></div></div></section>`; }
  function mountShell() { if (q("#fund-manager")) return; const sourceStrip = q(".source-strip"); if (sourceStrip) sourceStrip.insertAdjacentHTML("afterend", shellMarkup()); }
  function renderFileList() { const list=q("#fm-file-list"), clear=q("#fm-clear-files"), analyse=q("#fm-analyse"), count=q("#fm-file-count"); if(!list)return; if(!state.files.length){list.innerHTML="";clear.hidden=true;analyse.disabled=true;count.textContent="";return;} list.innerHTML=state.files.map((f,i)=>`<div class="fm-file-row"><span class="fm-file-name" title="${esc(f.name)}">${esc(f.name)}</span><span class="fm-file-size">${esc(humanSize(f.size))}</span><button type="button" data-fm-remove="${i}" aria-label="Remove ${esc(f.name)}">✕</button></div>`).join(""); clear.hidden=false; analyse.disabled=state.busy; count.textContent=`${state.files.length} file${state.files.length===1?"":"s"} selected`; }
  function addFiles(fileList) { const keys=new Set(state.files.map(f=>`${f.name}:${f.size}`)); for(const f of [...fileList]){const k=`${f.name}:${f.size}`;if(!keys.has(k)){state.files.push(f);keys.add(k);}} renderFileList(); }
  function renderInventory(){const target=q("#fm-inventory");if(!target)return;if(!state.sources){target.innerHTML="";return;}const{sources,source_count:count,unknown_count:unknown}=state.sources;if(!count){target.innerHTML='<div class="fm-empty">No sources identified yet.</div>';return;}const cards=sources.map(s=>{const warnings=(s.warnings||[]).map(w=>`<div class="fm-warning">${esc(w)}</div>`).join("");const hash=s.sha256?`${esc(s.sha256.slice(0,10))}…`:"—";return `<div class="fm-source-card${s.status==="processed"?"":` ${esc(s.status)}`}"><div class="fm-source-icon">${esc(s.id.replace("SRC-",""))}</div><div class="fm-source-body"><strong title="${esc(s.filename)}">${esc(s.filename)}</strong><p>${esc(typeLabel(s.detected_type))}</p>${warnings}</div><div style="display:grid;gap:8px;justify-items:end;">${statusPill(s.status)}<span class="fm-source-hash">${hash}</span></div></div>`;}).join("");target.innerHTML=`<div class="fm-inventory"><div class="fm-inventory-head"><h3>Evidence inventory</h3><span>${esc(count)} file${count===1?"":"s"} · ${esc(unknown)} need${unknown===1?"s":""} review</span></div><div class="fm-source-grid">${cards}</div><div class="fm-boundary">Classification only identifies what was uploaded — it does not decide which controls to run or review the figures inside. No control has run yet.</div></div>`;}
  function buildEvidenceForm(){const form=new FormData();for(const file of state.files)form.append("files",file,file.name);return form;}
  async function postEvidence(path,form){const now=Date.now(),last=state.lastRequestAt.get(path)||0,remaining=REQUEST_COOLDOWN_MS-(now-last);if(remaining>0)throw new Error(`Please wait ${Math.ceil(remaining/1000)} seconds before retrying this action.`);state.lastRequestAt.set(path,now);const response=await fetch(path,{method:"POST",body:form});let body={};try{body=await response.json();}catch{body={};}if(!response.ok){const detail=typeof body.detail==="string"?body.detail:body.detail?.message||`${response.status} ${response.statusText}`;throw new Error(detail);}return body;}
  async function runAnalysis(){if(!state.files.length||state.busy)return;busy(true);try{const body=await postEvidence("/api/fund-manager/analyse",buildEvidenceForm());state.analysis=body;state.sources={sources:body.sources,source_count:body.sources.length,unknown_count:body.sources.filter(s=>s.status!=="processed").length};renderInventory();renderAnalysis();notify(body.status==="clean"?"Analysis complete — no issues found among the controls that ran.":`Analysis complete — ${body.issues_found} issue${body.issues_found===1?"":"s"} found.`,body.status!=="clean");}catch(error){notify(error.message||"Could not analyse the uploaded files.",true);}finally{busy(false);}}
  function severityPill(severity){const label=severity.charAt(0).toUpperCase()+severity.slice(1);if(severity==="high")return `<span class="fm-pill error">${esc(label)}</span>`;if(severity==="medium")return `<span class="fm-pill review">${esc(label)}</span>`;return `<span class="fm-pill ok">${esc(label)}</span>`;}
  function controlPlanStatusPill(entry){if(entry.status==="executed")return '<span class="fm-pill ok">Executed</span>';if(entry.status==="failed")return '<span class="fm-pill error">Failed</span>';if(entry.status==="needs_evidence"){const missing=(entry.missing_evidence||[]).filter(v=>v!=="registered deterministic execution adapter");if(missing.length)return `<span class="fm-pill review" title="Missing: ${esc(missing.join(", "))}">Awaiting evidence</span>`;return '<span class="fm-pill review">Adapter pending</span>';}return `<span class="fm-pill review">${esc(entry.status||"Review")}</span>`;}
  function renderAnalysis(){const target=q("#fm-analysis");if(!target)return;if(!state.analysis){target.innerHTML="";return;}const{status,issues_found:issuesFound,material,critical,issues,control_plan:plan}=state.analysis;const statusClass=status==="clean"?"ok":"review",statusLabel=status==="clean"?"Clean":status==="partially_evaluated"?"Partially evaluated":"Review required";const issueCards=(issues||[]).map(issue=>{const evidence=(issue.evidence||[]).map(item=>`<li>${esc(item.source)} — ${esc(item.detail)}</li>`).join("");return `<div class="fm-issue-card"><div class="fm-issue-head"><strong>${esc(issue.title)}</strong>${severityPill(issue.severity)}</div><p>${esc(issue.summary||issue.detail)}</p>${evidence?`<ul class="fm-issue-evidence">${evidence}</ul>`:""}${issue.recommended_action?`<p class="fm-issue-action"><strong>Recommended action:</strong> ${esc(issue.recommended_action)}</p>`:""}</div>`;}).join("");const planRows=(plan||[]).map(entry=>{const missing=(entry.missing_evidence||[]).filter(v=>v!=="registered deterministic execution adapter");const detail=entry.status==="needs_evidence"&&missing.length?`<small>Needs: ${esc(missing.join(", "))}</small>`:entry.status==="needs_evidence"?'<small>Deterministic adapter is pending.</small>':"";return `<div class="fm-plan-row"><span class="fm-plan-file">${esc((entry.source_ids||[]).join(", "))}</span><span class="fm-plan-control">${esc(entry.control)}${detail}</span>${controlPlanStatusPill(entry)}</div>`;}).join("");target.innerHTML=`<div class="fm-analysis"><div class="fm-qc-banner ${statusClass}"><div><span class="fm-kicker">Fund administrator QC report</span><strong>${esc(statusLabel)}</strong></div><div class="fm-qc-metrics"><div><span>Issues found</span><strong>${esc(issuesFound)}</strong></div><div><span>Material</span><strong>${esc(material)}</strong></div><div><span>Critical</span><strong>${esc(critical)}</strong></div></div></div>${issueCards?`<div class="fm-issue-grid">${issueCards}</div>`:'<div class="fm-empty">No issues from the controls that ran.</div>'}<details class="fm-control-plan"><summary>Control plan (${(plan||[]).length} control${(plan||[]).length===1?"":"s"})</summary><div class="fm-plan-grid">${planRows}</div></details><div class="fm-boundary">Executed controls ran deterministically. “Awaiting evidence” means a required companion source was not uploaded; “Adapter pending” means the source is recognised but that deterministic control has not been wired yet. Neither is treated as a pass.</div></div>`;}
  function wireEvents(){const dropzone=q("#fm-dropzone"),input=q("#fm-file-input"),browse=q("#fm-browse");browse?.addEventListener("click",()=>input?.click());input?.addEventListener("change",e=>{addFiles(e.target.files);e.target.value="";});["dragenter","dragover"].forEach(n=>dropzone?.addEventListener(n,e=>{e.preventDefault();dropzone.classList.add("dragover");}));["dragleave","drop"].forEach(n=>dropzone?.addEventListener(n,e=>{e.preventDefault();dropzone.classList.remove("dragover");}));dropzone?.addEventListener("drop",e=>{if(e.dataTransfer?.files?.length)addFiles(e.dataTransfer.files);});q("#fm-file-list")?.addEventListener("click",e=>{const button=e.target.closest("[data-fm-remove]");if(!button)return;state.files.splice(Number(button.dataset.fmRemove),1);renderFileList();});q("#fm-clear-files")?.addEventListener("click",()=>{state.files=[];state.sources=null;state.analysis=null;renderFileList();renderInventory();renderAnalysis();});q("#fm-analyse")?.addEventListener("click",runAnalysis);}
  function init(){injectStyles();mountShell();wireEvents();renderFileList();}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init);else init();
})();
